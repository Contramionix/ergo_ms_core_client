/**
 * Пакет @ergo-ms/client-shell — публичные экспорты для внешних SPA модулей.
 */
export { bootstrapErgoClient, normalizeClientModuleManifest, registerClientModule } from './bootstrapErgoClient.js'
